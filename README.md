# Twenty Twenty Child Theme && docker-compose file
A boilerplate child theme for Twenty Twenty WordPress theme and a docker-compose.yml file to fire up the server

To fire up the wordpress server: 
docker compose up -d
